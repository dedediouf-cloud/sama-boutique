diff --git a/prisma/schema.prisma b/prisma/schema.prisma
index 2176246..6560fdd 100644
--- a/prisma/schema.prisma
+++ b/prisma/schema.prisma
@@ -61,6 +61,9 @@ model StockEntry {
 
   userId String
   user   User   @relation(fields: [userId], references: [id])
+
+  @@index([userId])
+  @@index([productId, createdAt])
 }
 
 model Employee {
@@ -75,6 +78,9 @@ model Employee {
   user      User     @relation(fields: [userId], references: [id])
 
   sales     Sale[]
+
+  @@index([userId])
+  @@index([email])
 }
 
 model Product {
@@ -86,6 +92,7 @@ model Product {
   lowStock    Int      @default(5)
   category    String?
   imageUrl    String?
+  barcode     String?  @unique   // Code-barres EAN-13 / QR pour scan
   supplierId  String?
   createdAt   DateTime @default(now())
   updatedAt   DateTime @updatedAt
@@ -99,6 +106,10 @@ model Product {
   saleItems    SaleItem[]
   stockEntries StockEntry[]
   inventoryCounts InventoryCount[]
+
+  @@index([userId])
+  @@index([userId, createdAt])
+  @@index([userId, category])
 }
 
 model Customer {
@@ -118,6 +129,10 @@ model Customer {
 
   sales         Sale[]
   reservations  Reservation[]
+
+  @@index([userId])
+  @@index([userId, createdAt])
+  @@index([userId, fidelityPoints])
 }
 
 model Promotion {
@@ -137,6 +152,9 @@ model Promotion {
   user   User   @relation(fields: [userId], references: [id])
 
   sales Sale[]
+
+  @@index([userId])
+  @@index([userId, active])
 }
 
 model Sale {
@@ -169,6 +187,11 @@ model Sale {
   items         SaleItem[]
   transactions  PaymentTransaction[]
   delivery      Delivery?
+
+  @@index([userId])
+  @@index([userId, createdAt])
+  @@index([userId, paymentStatus])
+  @@index([userId, customerId])
 }
 
 model PaymentTransaction {
@@ -200,6 +223,10 @@ model Delivery {
 
   userId String
   user   User   @relation(fields: [userId], references: [id])
+
+  @@index([userId])
+  @@index([userId, createdAt])
+  @@index([saleId])
 }
 
 model SaleItem {
@@ -229,6 +256,8 @@ model Supplier {
   orders   SupplierOrder[]
   // ✅ Relation inverse nommée (doit matcher exactement le nom dans Product)
   products Product[] @relation("SupplierProducts")
+
+  @@index([userId])
 }
 
 model SupplierOrder {
@@ -248,6 +277,9 @@ model SupplierOrder {
   user   User   @relation(fields: [userId], references: [id])
 
   items SupplierOrderItem[]
+
+  @@index([userId])
+  @@index([userId, createdAt])
 }
 
 model SupplierOrderItem {
@@ -280,6 +312,10 @@ model Reservation {
 
   customerId String?
   customer   Customer? @relation(fields: [customerId], references: [id])
+
+  @@index([userId])
+  @@index([userId, createdAt])
+  @@index([userId, status])
 }
 
 model SuperAdmin {
@@ -305,6 +341,8 @@ model InventoryCount {
 
   userId String
   user   User     @relation(fields: [userId], references: [id])
+
+  @@index([userId])
 }
 
 model SubscriptionPayment {
@@ -320,6 +358,9 @@ model SubscriptionPayment {
 
   userId String
   user   User     @relation(fields: [userId], references: [id])
+
+  @@index([userId])
+  @@index([userId, paidAt])
 }
 
 model SubscriptionPromotion {
@@ -397,6 +438,10 @@ model CashSession {
   updatedAt      DateTime  @updatedAt
 
   sales          Sale[]
+
+  @@index([userId])
+  @@index([userId, openedAt])
+  @@index([userId, status])
 }
 
 model TrialReminder {
diff --git a/prisma/schema.prod.prisma b/prisma/schema.prod.prisma
index e81fc70..9974ef3 100644
--- a/prisma/schema.prod.prisma
+++ b/prisma/schema.prod.prisma
@@ -61,6 +61,9 @@ model StockEntry {
 
   userId String
   user   User   @relation(fields: [userId], references: [id])
+
+  @@index([userId])
+  @@index([productId, createdAt])
 }
 
 model Employee {
@@ -75,6 +78,9 @@ model Employee {
   user      User     @relation(fields: [userId], references: [id])
 
   sales     Sale[]
+
+  @@index([userId])
+  @@index([email])
 }
 
 model Product {
@@ -100,6 +106,10 @@ model Product {
   saleItems    SaleItem[]
   stockEntries StockEntry[]
   inventoryCounts InventoryCount[]
+
+  @@index([userId])
+  @@index([userId, createdAt])
+  @@index([userId, category])
 }
 
 model Customer {
@@ -119,6 +129,10 @@ model Customer {
 
   sales         Sale[]
   reservations  Reservation[]
+
+  @@index([userId])
+  @@index([userId, createdAt])
+  @@index([userId, fidelityPoints])
 }
 
 model Promotion {
@@ -138,6 +152,9 @@ model Promotion {
   user   User   @relation(fields: [userId], references: [id])
 
   sales Sale[]
+
+  @@index([userId])
+  @@index([userId, active])
 }
 
 model Sale {
@@ -170,6 +187,11 @@ model Sale {
   items         SaleItem[]
   transactions  PaymentTransaction[]
   delivery      Delivery?
+
+  @@index([userId])
+  @@index([userId, createdAt])
+  @@index([userId, paymentStatus])
+  @@index([userId, customerId])
 }
 
 model PaymentTransaction {
@@ -201,6 +223,10 @@ model Delivery {
 
   userId String
   user   User   @relation(fields: [userId], references: [id])
+
+  @@index([userId])
+  @@index([userId, createdAt])
+  @@index([saleId])
 }
 
 model SaleItem {
@@ -230,6 +256,8 @@ model Supplier {
   orders   SupplierOrder[]
   // ✅ Relation inverse nommée (doit matcher exactement le nom dans Product)
   products Product[] @relation("SupplierProducts")
+
+  @@index([userId])
 }
 
 model SupplierOrder {
@@ -249,6 +277,9 @@ model SupplierOrder {
   user   User   @relation(fields: [userId], references: [id])
 
   items SupplierOrderItem[]
+
+  @@index([userId])
+  @@index([userId, createdAt])
 }
 
 model SupplierOrderItem {
@@ -281,6 +312,10 @@ model Reservation {
 
   customerId String?
   customer   Customer? @relation(fields: [customerId], references: [id])
+
+  @@index([userId])
+  @@index([userId, createdAt])
+  @@index([userId, status])
 }
 
 model SuperAdmin {
@@ -306,6 +341,8 @@ model InventoryCount {
 
   userId String
   user   User     @relation(fields: [userId], references: [id])
+
+  @@index([userId])
 }
 
 model SubscriptionPayment {
@@ -321,6 +358,9 @@ model SubscriptionPayment {
 
   userId String
   user   User     @relation(fields: [userId], references: [id])
+
+  @@index([userId])
+  @@index([userId, paidAt])
 }
 
 model SubscriptionPromotion {
@@ -398,6 +438,10 @@ model CashSession {
   updatedAt      DateTime  @updatedAt
 
   sales          Sale[]
+
+  @@index([userId])
+  @@index([userId, openedAt])
+  @@index([userId, status])
 }
 
 model TrialReminder {
diff --git a/src/app/api/deliveries/route.ts b/src/app/api/deliveries/route.ts
index 325cb57..254a148 100644
--- a/src/app/api/deliveries/route.ts
+++ b/src/app/api/deliveries/route.ts
@@ -3,23 +3,62 @@ import { prisma } from "@/lib/prisma";
 import { getCurrentUser } from "@/lib/session";
 import { assertWritable } from "@/lib/access";
 
-export async function GET() {
+/**
+ * GET /api/deliveries
+ *   (sans paramètre)  → 200 livraisons les plus récentes
+ *   ?limit=all        → tout l'historique
+ *
+ * ⚡ `select` ciblé : on ne renvoie que les champs affichés (payload ÷ 10).
+ */
+export async function GET(request?: Request) {
   const user = await getCurrentUser();
   if (!user) return NextResponse.json({ error: "Non authentifié" }, { status: 401 });
 
   const ownerId = user.ownerId || user.id;
 
+  let take: number | undefined = 200;
+  if (request) {
+    const brutal = new URL(request.url).searchParams.get("limit");
+    if (brutal === "all") take = undefined;
+    else if (brutal) {
+      const n = parseInt(brutal, 10);
+      if (Number.isFinite(n) && n > 0) take = Math.min(n, 2000);
+    }
+  }
+
   const deliveries = await prisma.delivery.findMany({
     where: { userId: ownerId },
-    include: {
+    take,
+    orderBy: { createdAt: "desc" },
+    select: {
+      id: true,
+      status: true,
+      address: true,
+      phone: true,
+      notes: true,
+      deliveryDate: true,
+      createdAt: true,
+      updatedAt: true,
+      saleId: true,
+      userId: true,
       sale: {
-        include: {
-          customer: true,
-          items: { include: { product: true } },
+        select: {
+          id: true,
+          total: true,
+          finalTotal: true,
+          createdAt: true,
+          customer: { select: { id: true, name: true, phone: true } },
+          items: {
+            select: {
+              id: true,
+              quantity: true,
+              price: true,
+              product: { select: { id: true, name: true } },
+            },
+          },
         },
       },
     },
-    orderBy: { createdAt: "desc" },
   });
 
   return NextResponse.json(deliveries);
diff --git a/src/app/api/products/route.ts b/src/app/api/products/route.ts
index e4ea8b8..a61ed99 100644
--- a/src/app/api/products/route.ts
+++ b/src/app/api/products/route.ts
@@ -9,9 +9,26 @@ export async function GET() {
 
   const ownerId = user.ownerId || user.id;
 
+  // ⚡ On ne renvoie que le NOM du fournisseur (l'interface n'affiche que ça),
+  //    au lieu de l'objet fournisseur complet dupliqué pour chaque produit.
   const products = await prisma.product.findMany({
     where: { userId: ownerId },
-    include: { supplier: true },
+    select: {
+      id: true,
+      name: true,
+      description: true,
+      price: true,
+      quantity: true,
+      lowStock: true,
+      category: true,
+      imageUrl: true,
+      barcode: true,
+      supplierId: true,
+      createdAt: true,
+      updatedAt: true,
+      userId: true,
+      supplier: { select: { id: true, name: true, phone: true } },
+    },
     orderBy: { createdAt: "desc" },
   });
 
diff --git a/src/app/api/sales/route.ts b/src/app/api/sales/route.ts
index 99fca7e..1e71e37 100644
--- a/src/app/api/sales/route.ts
+++ b/src/app/api/sales/route.ts
@@ -5,22 +5,71 @@ import { getPaymentProvider } from "@/lib/payments";
 import { getMerchantCredentials } from "@/lib/payments/credentials";
 import { assertWritable } from "@/lib/access";
 
-export async function GET() {
+/** Nombre de ventes renvoyées par défaut (les plus récentes) */
+export const VENTES_PAR_DEFAUT = 200;
+const VENTES_MAX = 2000;
+
+/**
+ * GET /api/sales
+ *   (sans paramètre)      → 200 ventes les plus récentes
+ *   ?limit=500            → 500 ventes les plus récentes
+ *   ?limit=all            → tout l'historique (exports, sauvegardes)
+ *
+ * ⚡ Optimisations :
+ *   - `take` : évite de renvoyer tout l'historique à chaque ouverture du POS
+ *   - `select` ciblé : on ne renvoie QUE les champs affichés par l'interface.
+ *     Les `transactions` et `promotion` ne sont jamais utilisés côté écran :
+ *     on ne les charge donc pas depuis la base (payload divisé par ~25).
+ */
+export async function GET(request?: Request) {
   const user = await getCurrentUser();
   if (!user) return NextResponse.json({ error: "Non authentifié" }, { status: 401 });
 
   const ownerId = user.ownerId || user.id;
 
+  let take: number | undefined = VENTES_PAR_DEFAUT;
+  if (request) {
+    const brutal = new URL(request.url).searchParams.get("limit");
+    if (brutal === "all") take = undefined;
+    else if (brutal) {
+      const n = parseInt(brutal, 10);
+      if (Number.isFinite(n) && n > 0) take = Math.min(n, VENTES_MAX);
+    }
+  }
+
   const sales = await prisma.sale.findMany({
     where: { userId: ownerId },
-    include: {
-      items: { include: { product: true } },
-      customer: true,
-      transactions: true,
-      delivery: true,
-      promotion: true,
-    },
+    take,
     orderBy: { createdAt: "desc" },
+    select: {
+      id: true,
+      total: true,
+      discount: true,
+      finalTotal: true,
+      paymentMethod: true,
+      paymentStatus: true,
+      paymentRef: true,
+      deliveryType: true,
+      earnedFidelityPoints: true,
+      createdAt: true,
+      userId: true,
+      customerId: true,
+      employeeId: true,
+      promotionId: true,
+      cashSessionId: true,
+      // Uniquement le nécessaire pour l'affichage de la liste
+      items: {
+        select: {
+          id: true,
+          quantity: true,
+          price: true,
+          productId: true,
+          product: { select: { id: true, name: true, price: true } },
+        },
+      },
+      customer: { select: { id: true, name: true, phone: true } },
+      delivery: { select: { id: true, status: true, address: true } },
+    },
   });
 
   return NextResponse.json(sales);
diff --git a/src/app/sales/page.tsx b/src/app/sales/page.tsx
index 659873a..ce54db1 100644
--- a/src/app/sales/page.tsx
+++ b/src/app/sales/page.tsx
@@ -125,6 +125,13 @@ export default function SalesPage() {
 
   const [expandedSaleId, setExpandedSaleId] = useState<string | null>(null);
 
+  // ⚡ Pagination : on ne charge que les ventes récentes (payload ÷ 25).
+  //    Le bouton « Charger plus » augmente cette limite.
+  const salesLimitRef = useRef(200);
+  const [salesLimit, setSalesLimit] = useState(200);
+  const [toutCharge, setToutCharge] = useState(false);
+  const [chargementPlus, setChargementPlus] = useState(false);
+
   const [cashSession, setCashSession] = useState<any>(null);
 
   const [showCashModal, setShowCashModal] = useState(false);
@@ -317,15 +324,20 @@ export default function SalesPage() {
 
   }, []);
 
-  const fetchSales = useCallback(async () => {
+  const fetchSales = useCallback(async (limite?: number) => {
 
     try {
 
-      const res = await fetch("/api/sales");
+      const l = limite ?? salesLimitRef.current;
+      const res = await fetch(`/api/sales?limit=${l}`);
 
       const data = await res.json();
 
-      if (res.ok && Array.isArray(data)) setSales(data);
+      if (res.ok && Array.isArray(data)) {
+        setSales(data);
+        // Si le serveur renvoie moins que demandé, on a tout l'historique
+        if (data.length < l) setToutCharge(true);
+      }
 
     } catch (err) {}
 
@@ -2184,6 +2196,24 @@ export default function SalesPage() {
 
               </h2>
 
+              {!toutCharge && sales.length > 0 && (
+                <button
+                  type="button"
+                  disabled={chargementPlus}
+                  onClick={async () => {
+                    setChargementPlus(true);
+                    const nouvelle = salesLimitRef.current + 300;
+                    salesLimitRef.current = nouvelle;
+                    setSalesLimit(nouvelle);
+                    await fetchSales(nouvelle);
+                    setChargementPlus(false);
+                  }}
+                  className="px-3 py-2 rounded-xl border border-[#D4AF37]/30 text-[#B87333] text-xs font-medium hover:bg-[#D4AF37]/10 transition-colors disabled:opacity-50"
+                >
+                  {chargementPlus ? "Chargement..." : `Charger plus (${sales.length} affichées)`}
+                </button>
+              )}
+
             </div>
 
             <div className="overflow-x-auto -mx-1 px-1 touch-pan-x">
